using System;

class VigenerePolialfabetic
{
    static string alfabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";

    static string[] chei = {
        "FRQSPYMHNJOELKDVAGXTIWBUZC",
        "SWZCINJTELAFQUMKPXDOVBRGHY",
        "CITYWLNZEVOMQGUPJFXBRAHSKD"
    };

    static void Main()
    {
        Console.WriteLine("Introdu textul:");
        string text = Console.ReadLine().ToUpper();

        Console.WriteLine("\nAlege operatia:");
        Console.WriteLine("1 - Criptare");
        Console.WriteLine("2 - Decriptare");
        Console.Write("Optiune: ");
        int optiune = int.Parse(Console.ReadLine());

        if (optiune == 1)
            Console.WriteLine("\nText criptat:\n" + Criptare(text));
        else if (optiune == 2)
            Console.WriteLine("\nText decriptat:\n" + Decriptare(text));
        else
            Console.WriteLine("Optiune invalida!");
    }

    static string Criptare(string text)
    {
        char[] rezultat = text.ToCharArray();
        int pozitie = 0;

        for (int i = 0; i < rezultat.Length; i++)
        {
            char c = rezultat[i];

            if (alfabet.Contains(c))
            {
                int indexAlfabet = alfabet.IndexOf(c);
                string cheieCurenta = chei[pozitie % 3];
                rezultat[i] = cheieCurenta[indexAlfabet];
                pozitie++;
            }
        }

        return new string(rezultat);
    }

    static string Decriptare(string text)
    {
        char[] rezultat = text.ToCharArray();
        int pozitie = 0;

        for (int i = 0; i < rezultat.Length; i++)
        {
            char c = rezultat[i];

            if (alfabet.Contains(c))
            {
                string cheieCurenta = chei[pozitie % 3];
                int index = cheieCurenta.IndexOf(c);
                rezultat[i] = alfabet[index];
                pozitie++;
            }
        }

        return new string(rezultat);
    }
}
